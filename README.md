##NoesisGui Qt Binding
**Requires:** NoesisGui >= 1.3, Qt >= 5.4

**Known issues:** QNoesisWidget might flick when resizing. Both QNoesisWindow and QNoesisWidget might consume high amount of CPU.

Pull requests are wellcomed.
